# NovaCart AI Assistant — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Python 3.10+ required
# ─────────────────────────────────────────────

# ── LLM & Agent Framework ────────────────────────────────────────────
langchain-groq==0.3.2               # Groq LLM client (llama-3.1-8b-instant)
langchain-core==0.3.51              # LangChain base (messages, runnables)
langgraph==0.3.21                   # LangGraph StateGraph workflow engine

# ── RAG Stack ────────────────────────────────────────────────────────
langchain-chroma==0.2.4             # ChromaDB vector store integration
langchain-huggingface==0.1.2        # HuggingFace embeddings (all-MiniLM-L6-v2)
langchain-community==0.3.20         # PDF loader (PyPDFLoader)
langchain-text-splitters==0.3.8     # RecursiveCharacterTextSplitter
chromadb==0.6.3                     # ChromaDB vector database
sentence-transformers==3.4.1        # HuggingFace sentence embeddings
pypdf==5.9.0                        # PDF parsing for document ingestion

# ── MCP Server ───────────────────────────────────────────────────────
fastmcp==2.3.3                      # FastMCP server + client for RAG tool

# ── Backend API ──────────────────────────────────────────────────────
fastapi==0.115.12                   # FastAPI web framework
uvicorn==0.34.0                     # ASGI server for FastAPI
pydantic==2.11.1                    # Data validation (used by FastAPI)

# ── Frontend ─────────────────────────────────────────────────────────
streamlit==1.44.0                   # Streamlit chatbot + dashboard UI
pandas==2.2.3                       # DataFrames for evaluation + observability tables
requests==2.32.3                    # HTTP client (Streamlit → FastAPI calls)

# ── Observability & Tracing ──────────────────────────────────────────
langsmith==0.3.27                   # LangSmith tracing (optional, needs LANGCHAIN_API_KEY)

# ── Utilities ────────────────────────────────────────────────────────
python-dotenv==1.0.1                # .env file loading